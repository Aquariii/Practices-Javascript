/* Solution in finding the last letter of the string */
function lastLetter(str){
    return alert(str.charAt(str.length1)); //Returns the last index of the string//
}
lastLetter(window.prompt("Enter a string"));

/* The Arrow function version of the above exercise */
/*(let lastLetter=(str)=>alert(str.charAt(str.length -1));
lastLetter(window.prompt("Enter a string"));*/